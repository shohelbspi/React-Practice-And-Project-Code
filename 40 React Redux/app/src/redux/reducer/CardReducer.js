const initialState =  {
    item:[],
    totalQty : 0
}

const cartReducre = (state=initialState,action)=>{
    if(action.type==='ADD_TO_CART'){
        return state
    }
    else{
        return state
    }

}

export default cartReducre